package com.example.projet_hotels;

import androidx.appcompat.app.AppCompatActivity;
import androidx.recyclerview.widget.LinearLayoutManager;
import androidx.recyclerview.widget.RecyclerView;

import android.os.Bundle;

public class afficherResultat extends AppCompatActivity {
    String s1[],s2[];
    //int imgages[];
    RecyclerView recyclerView;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_afficher_resultat);
       // s1 = new String[]{"new beach","KT hotel"};
       // s2 = new String[]{"new beach","KT hotel"};
        //imgages = new int[]{R.drawable.ic_hotel,R.drawable.ic_hotel};
       // recyclerView = findViewById(R.id.resultatShow);
        //Myadapter myadapter = new Myadapter(this,s1,s2);
       // recyclerView.setAdapter(myadapter);
       // recyclerView.setLayoutManager(new LinearLayoutManager(this));
       // LinearLayoutManager llm = new LinearLayoutManager(this);

    }
}